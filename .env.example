# Random 32-byte secret for cookie signing. Generate with:
#   openssl rand -base64 32
SESSION_SECRET=

# Shared password for both players.
APP_PASSWORD=

# Provisioned automatically by the Vercel Neon Marketplace integration.
# For local dev: `vercel env pull` after linking the project.
DATABASE_URL=
